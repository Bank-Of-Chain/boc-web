name: CICD
on:
  pull_request:
    branches:
      - '**'
jobs:
  release:
    name: testcases runner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install
        uses: borales/actions-yarn@master
        with:
          cmd: install
      - name: Release
        uses: borales/actions-yarn@master
        with:
          cmd: test
      