import wallet from '@/helpers/wallet'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the wallet utils should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
