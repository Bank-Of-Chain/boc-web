import { hasMetamaskInstalled } from '@/helpers/plugin-util'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('hasMetamaskInstalled', () => {
    //TODO:
    hasMetamaskInstalled()
  })
})
