import useABTest from '@/hooks/useABTest'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the useABTest hooks should be render correct', () => {
    expect(1 === 1).toBe(true)
  })
})
