import useVersionWapper from '@/hooks/useVersionWapper'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the useVersionWapper hooks should be render correct', () => {
    expect(1 === 1).toBe(true)
  })
})
