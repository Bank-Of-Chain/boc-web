import useErc20Token from '@/hooks/useErc20Token'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the useErc20Token hooks should be render correct', () => {
    expect(1 === 1).toBe(true)
  })
})
