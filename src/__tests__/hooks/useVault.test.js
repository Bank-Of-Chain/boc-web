import useVault from '@/hooks/useVault'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the useVault hooks should be render correct', () => {
    expect(1 === 1).toBe(true)
  })
})
