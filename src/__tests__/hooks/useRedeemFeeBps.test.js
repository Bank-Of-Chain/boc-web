import useRedeemFeeBps from '@/hooks/useRedeemFeeBps'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the useRedeemFeeBps hooks should be render correct', () => {
    expect(1 === 1).toBe(true)
  })
})
