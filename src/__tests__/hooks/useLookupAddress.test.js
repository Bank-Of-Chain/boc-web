import useLookupAddress from '@/hooks/useLookupAddress'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the useLookupAddress hooks should be render correct', () => {
    expect(1 === 1).toBe(true)
  })
})
