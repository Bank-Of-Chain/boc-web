import abtest from '@/constants/abtest'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the abtest constants should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
