import wallet from '@/constants/wallet'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the wallet constants should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
