import apyDuration from '@/constants/apy-duration'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the apy-duration constants should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
