import ethi from '@/constants/ethi'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the ethi constants should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
