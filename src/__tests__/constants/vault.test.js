import vault from '@/constants/vault'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the vault constants should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
