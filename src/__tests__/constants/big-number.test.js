import bigNumber from '@/constants/big-number'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the bigNumber constants should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
