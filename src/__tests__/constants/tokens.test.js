import tokens from '@/constants/tokens'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the tokens constants should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
