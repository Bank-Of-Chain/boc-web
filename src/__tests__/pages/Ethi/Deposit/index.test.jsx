import EthiDeposit from '@/pages/Ethi/Deposit'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the component should be render', () => {
    expect(1 === 1).toBe(true)
  })
})
