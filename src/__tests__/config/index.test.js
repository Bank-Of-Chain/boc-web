import config from '@/config/config'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the config item should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
