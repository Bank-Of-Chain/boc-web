import apiService from '@/services/api-service'
import { describe, expect, it } from 'vitest'

describe('xxxxxxxx', () => {
  it('the api-service services should be correct', () => {
    expect(1 === 1).toBe(true)
  })
})
